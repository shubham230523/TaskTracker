<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Allows us to override platform level specific attributes in their
         respective values-vXX folder. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <!-- The actual theme we use. This varies for light theme (here),
         and values-night for dark theme. -->
    <style name="TaskTrackerTheme" parent="AppTheme">
        <item name="colorPrimary">@color/Teal400</item>
        <item name="colorPrimaryDark">@color/Teal600</item>
        <item name="colorAccent">@color/Blue600</item>
    </style>

<!--    <style name="TaskTrackerTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />-->

    <style name="TaskTrackerTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

</resources>