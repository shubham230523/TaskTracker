<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">
    <!-- Current date -->
    <TextView
        android:id="@+id/tv_month"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:elevation="5dp"
        android:fontFamily="@font/robotoslab_medium"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    <!-- Recyclerview for selecting different dates -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_dates"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="10dp"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_month" />
    <!-- Button "task missed" -->
    <TextView
        android:id="@+id/btnTaskMissed"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:background="@drawable/dates_unselected_background_stroke"
        android:elevation="5dp"
        app:layout_constraintEnd_toStartOf="@id/btnUpcommingTasks"
        android:fontFamily="@font/robotoslab_regular"
        android:text="Missed"
        app:layout_constraintTop_toBottomOf="@id/rv_dates"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="10dp"
        android:textAlignment="center"
        android:textColor="@color/grey"
        android:textSize="16sp" />
    <!-- Button upcoming tasks -->
    <TextView
        android:id="@+id/btnUpcommingTasks"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:background="@drawable/dates_unselected_background_stroke"
        android:elevation="5dp"
        android:fontFamily="@font/robotoslab_regular"
        android:text="Upcoming"
        app:layout_constraintEnd_toStartOf="@id/btnAllUpcomingTasks"
        app:layout_constraintTop_toBottomOf="@id/rv_dates"
        app:layout_constraintStart_toEndOf="@id/btnTaskMissed"
        android:layout_marginTop="10dp"
        android:textAlignment="center"
        android:textColor="@color/grey"
        android:textSize="16sp" />
    <!-- Button "task missed" -->
    <TextView
        android:id="@+id/btnAllUpcomingTasks"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:background="@drawable/dates_unselected_background_stroke"
        android:elevation="5dp"
        android:fontFamily="@font/robotoslab_regular"
        android:text="All Upcoming"
        app:layout_constraintTop_toBottomOf="@id/rv_dates"
        app:layout_constraintStart_toEndOf="@id/btnUpcommingTasks"
        android:layout_marginTop="10dp"
        android:textAlignment="center"
        android:textColor="@color/grey"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"/>
    <!-- Recyclerview showing upcoming tasks -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_tasks"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="5dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnTaskMissed"/>
</androidx.constraintlayout.widget.ConstraintLayout>